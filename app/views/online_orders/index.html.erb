<div class="container py-5 online-orders z-10">
  <header class="text-center mb-5">
    <span class="badge bg-light text-dark border rounded-pill px-3 py-2 mb-2">Comenzi online</span>
    <h1 class="fw-bold text-dark">Deserturi disponibile pentru comenzi online</h1>
    <p class="text-muted mb-0">Descoperă selecția de produse pe care le poți comanda direct din site, grupate pe categorii.</p>
  </header>

  <% if @categories_with_recipes.any? %>
    <% @categories_with_recipes.each do |category, recipes| %>
      <section class="online-orders__category mb-5">
        <div class="d-flex flex-wrap justify-content-between align-items-baseline gap-2 mb-3">
          <div>
            <h2 class="h4 mb-1 text-dark"><%= category&.name || 'Alte produse' %></h2>
            <p class="text-muted small mb-0">Disponibile imediat pentru comandă online</p>
          </div>
          <% if category.present? %>
            <%= link_to 'Vezi toate produsele din categorie', category_recipes_path(category), class: 'btn btn-outline-secondary btn-sm' %>
          <% end %>
        </div>

        <div class="cards-main-container online-orders__cards">
          <%= render partial: 'recipes/recipe', collection: recipes, as: :recipe %>
        </div>
      </section>
    <% end %>
  <% else %>
    <div class="card border-0 shadow-sm">
      <div class="card-body p-5 text-center">
        <div class="display-4 text-muted mb-3">🕒</div>
        <h2 class="h5 text-dark mb-2">Momentan nu sunt produse disponibile online.</h2>
        <p class="text-muted mb-4">Revino în curând sau contacta-ne pentru recomandări personalizate.</p>
        <%= link_to 'Vezi toate categoriile', categories_path, class: 'btn btn-outline-primary' %>
      </div>
    </div>
  <% end %>
</div>
