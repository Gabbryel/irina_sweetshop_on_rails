<% if @cakemodel %>
  <%= link_to 'Înapoi', category_path(@cakemodel.category), class: 'btn btn-sm button-default button-rounded' %>
<% elsif @recipe %>
  <%= link_to 'Înapoi', category_path(@recipe.category), class: 'btn btn-sm button-default button-rounded' %>
<% elsif @cakemodels || @recipes %>
  <%= link_to 'Înapoi', categories_path, class: 'btn btn-sm button-default button-rounded fonts-family color-fonts my-3' %>  
<% end %>
