<div class="container py-5" style="margin-top: 20dvh;">
  <div class="d-flex flex-wrap justify-content-between align-items-center mb-4 gap-2">
    <div>
      <h1 class="h4 mb-1" style="color: #1e293b; font-weight: 600;">Administrare utilizatori</h1>
      <p class="text-muted small mb-0">Vezi, sortează și gestionează toate conturile de utilizator.</p>
    </div>
    <div class="d-flex gap-2 align-items-center">
      <div class="btn-group btn-group-sm" role="group" aria-label="Sort users">
        <%= link_to 'După dată', dashboard_admin_users_path(sort: 'created_at', direction: @direction == 'asc' && @sort == 'created_at' ? 'desc' : 'asc'), class: "btn btn-sm", style: "#{@sort == 'created_at' ? 'background: #1e293b; color: white;' : 'background: white; color: #64748b; border: 1px solid #e5e7eb;'} border-radius: 6px;" %>
        <%= link_to 'După email', dashboard_admin_users_path(sort: 'email', direction: @direction == 'asc' && @sort == 'email' ? 'desc' : 'asc'), class: "btn btn-sm", style: "#{@sort == 'email' ? 'background: #1e293b; color: white;' : 'background: white; color: #64748b; border: 1px solid #e5e7eb;'} border-radius: 6px;" %>
        <%= link_to 'După rol', dashboard_admin_users_path(sort: 'role', direction: @direction == 'asc' && @sort == 'role' ? 'desc' : 'asc'), class: "btn btn-sm", style: "#{@sort == 'role' ? 'background: #1e293b; color: white;' : 'background: white; color: #64748b; border: 1px solid #e5e7eb;'} border-radius: 6px;" %>
      </div>
      <%= link_to 'Adaugă utilizator', new_dashboard_admin_user_path, class: 'btn btn-sm', style: 'background: #1e293b; color: white; border-radius: 6px;' %>
    </div>
  </div>

  <div class="card" style="border: 1px solid #e5e7eb; border-radius: 8px;">
    <div class="table-responsive">
      <table class="table table-sm align-middle mb-0">
        <thead style="background: #f8fafc;">
          <tr>
            <th>Email</th>
            <th>Rol</th>
            <th>Creat</th>
            <th class="text-end">Acțiuni</th>
          </tr>
        </thead>
        <tbody>
          <% if @users.any? %>
            <% @users.each do |user| %>
              <tr>
                <td class="fw-semibold"><%= mail_to user.email %></td>
                <td>
                  <% if user.admin? %>
                    <span class="badge" style="background: #1e293b; color: white;">Admin</span>
                  <% else %>
                    <span class="badge" style="background: #f1f5f9; color: #64748b;">Client</span>
                  <% end %>
                </td>
                <td class="text-muted small"><%= l(user.created_at, format: :short) %></td>
                <td class="text-end">
                  <div class="btn-group btn-group-sm" role="group" aria-label="User actions">
                    <%= link_to 'Vezi', dashboard_admin_user_path(user), class: 'btn btn-sm', style: 'background: white; color: #475569; border: 1px solid #e5e7eb; border-radius: 6px;' %>
                    <%= link_to 'Editează', edit_dashboard_admin_user_path(user), class: 'btn btn-sm', style: 'background: white; color: #475569; border: 1px solid #e5e7eb; border-radius: 6px;' %>
                    <% if user != current_user %>
                      <% if user.admin? %>
                        <%= button_to 'Retrage admin', toggle_admin_dashboard_admin_user_path(user), method: :patch, data: { confirm: 'Sigur vrei să retragi dreptul de administrator?' }, class: 'btn btn-sm', style: 'background: white; color: #475569; border: 1px solid #cbd5e1; border-radius: 6px;' %>
                      <% else %>
                        <%= button_to 'Promovează', toggle_admin_dashboard_admin_user_path(user), method: :patch, data: { confirm: 'Sigur vrei să promovezi utilizatorul la administrator?' }, class: 'btn btn-sm', style: 'background: #1e293b; color: white; border: none; border-radius: 6px;' %>
                      <% end %>
                      <%= button_to 'Șterge', dashboard_admin_user_path(user), method: :delete, data: { confirm: 'Ești sigur că vrei să ștergi acest utilizator? Această acțiune nu poate fi anulată.' }, class: 'btn btn-sm', style: 'background: white; color: #475569; border: 1px solid #cbd5e1; border-radius: 6px;' %>
                    <% end %>
                  </div>
                </td>
              </tr>
            <% end %>
          <% else %>
            <tr>
              <td colspan="4" class="text-center text-muted py-4">Nu există utilizatori.</td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
