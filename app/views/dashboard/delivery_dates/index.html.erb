<div class="container py-5 margin-top-20dvh">
  <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3 mb-4">
    <div>
      <span class="badge rounded-pill px-3 py-2 mb-2" style="background: #f1f5f9; color: #475569; font-weight: 600;">Dashboard</span>
      <h1 class="h3 mb-1" style="color: #1e293b; font-weight: 600;">Calendar livrări</h1>
      <p class="text-muted small mb-0">
        Click pe o zi pentru a o marca ca disponibilă (verde) sau indisponibilă (roșu). Datele la mai mult de <%= @max_window_days %> de zile sunt blocate automat.
      </p>
    </div>
    <div class="text-md-end">
      <p class="text-muted small mb-1">Interval activ</p>
      <p class="h4 mb-0"><%= l(@start_date) %> – <%= l(@end_date) %></p>
    </div>
  </div>

  <div class="card" style="border: 1px solid #e5e7eb; border-radius: 8px;">
    <div class="card-body">
      <div class="row g-3 mb-4">
        <div class="col-12 col-lg-6">
          <div class="p-3 rounded-3 h-100" style="border: 1px solid #e5e7eb; background: #f8fafc;">
            <h2 class="h6 mb-3">Blochează un interval rapid</h2>
            <%= form_with url: block_range_dashboard_delivery_dates_path, method: :post, data: { turbo: false }, class: "row g-2" do |f| %>
              <div class="col-12 col-sm-4">
                <%= label_tag :days, 'Număr zile', class: 'form-label small fw-semibold' %>
                <%= number_field_tag :days, nil, min: 1, class: 'form-control', required: true %>
              </div>
              <div class="col-12 col-sm-4">
                <%= label_tag :start_date, 'Începând cu', class: 'form-label small fw-semibold' %>
                <%= date_field_tag :start_date, (Date.current + 1.day).iso8601, class: 'form-control' %>
              </div>
              <div class="col-12 col-sm-4">
                <%= label_tag :reason, 'Motiv (opțional)', class: 'form-label small fw-semibold' %>
                <%= text_field_tag :reason, nil, class: 'form-control' %>
              </div>
              <div class="col-12">
                <%= submit_tag 'Blochează zilele', class: 'btn w-100', style: 'background: #1e293b; color: white; border: none; border-radius: 6px;' %>
              </div>
            <% end %>
            <p class="text-muted small mb-0 mt-2">Se vor bloca zile consecutive începând cu data aleasă.</p>
          </div>
        </div>
      </div>
      <%= turbo_frame_tag "delivery-calendar-frame" do %>
        <%= render partial: "calendar",
                   locals: { start_date: @start_date,
                             end_date: @end_date,
                             blocked_dates: @blocked_dates,
                             max_window_days: @max_window_days } %>
      <% end %>
    </div>
  </div>
</div>
