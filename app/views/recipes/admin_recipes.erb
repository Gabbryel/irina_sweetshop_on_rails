<div class="cards-main-container">
  <%= form_with model: @recipe do |form| %>
  <%= form.text_field :title %>
  <%= form.submit %>
<% end %>
    <%== pagy_bootstrap_nav(@pagy) %>
    <h3 class='text-center'>Administrare rețete(<%= @recipes.count %>)</h3>
    <%= render @recipes %>
    <%== pagy_bootstrap_nav(@pagy) %>
  </div>